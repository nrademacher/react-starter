{
  "name": "webdev-starter",
  "version": "0.0.1",
  "description": "Boilerplate for webpack-based sites or apps",
  "main": "index.js",
  "repository": "https://github.com/cxxiii/webdev-starter",
  "author": "Nikolay Rademacher",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "yarn build:tailwind && webpack serve --open --config webpack.dev.js",
    "build": "export NODE_ENV=production && yarn build:tailwind && yarn minify:css && webpack --config webpack.prod.js",
    "build:tailwind": "if test -f \"tailwind.source.css\"; then postcss tailwind.source.css -o src/style.css; fi",
    "clean": "yarn clean:build && yarn clean:css",
    "clean:build": "if test -d 'dist/'; then rm -r dist/; fi",
    "clean:css": "echo '' > src/style.css",
    "minify:css": "./node_modules/.bin/cleancss -o src/style.css src/style.css",
    "lint": "if test -f \".eslintrc.*\"; then eslint . --fix; fi",
    "setup:react": "yarn add react react-dom && yarn add -D @babel/preset-react && echo '{ \"presets\": [\"@babel/preset-react\"] }' > .babelrc",
    "setup:eslint": "yarn add -D eslint && eslint --init && if test -f \"package-lock.json\"; then rm package-lock.json && yarn; fi",
    "eject:react": "yarn remove react react-dom @babel/preset-react && rm .babelrc"
  },
  "devDependencies": {
    "@babel/core": "^7.13.8",
    "@babel/preset-env": "^7.13.8",
    "babel-loader": "^8.2.2",
    "clean-css-cli": "^5.2.2",
    "clean-webpack-plugin": "^3.0.0",
    "html-loader": "^2.1.2",
    "html-webpack-plugin": "^5.2.0",
    "postcss": "^8.2.8",
    "postcss-cli": "^8.3.1",
    "webpack": "^5.23.0",
    "webpack-cli": "^4.5.0",
    "webpack-dev-server": "^3.11.2",
    "webpack-merge": "^5.7.3"
  },
  "dependencies": {
    "autoprefixer": "^10.2.5",
    "tailwindcss": "^2.0.4"
  }
}
